<!DOCTYPE html>
<html lang="en">
<head>
  {{> head-prelude}}
  <title>Privacy Policy</title>
  {{#with page.canonicalUrl}}
    <link rel="canonical" href="{{this}}">
  {{/with}}
  {{> head-styles}}
  {{> head-meta}}
  {{> head-scripts}}
  {{> head-icons}}
  <style>
    .privacy {
      padding: 20px;
      max-width: 1800px;
      margin: auto;
    }
    #privacy-policy {
      margin-top: 20px;
    }
    .loading {
      font-style: italic;
      color: var(--secondary-text);
      text-align: center;
    }
    .error {
      color: red;
      font-weight: bold;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      table-layout: fixed; /* Ensures equal columns */
      word-wrap: break-word;
    }

    th, td {
      padding: 10px;
      text-align: left;
      border-bottom: 1px solid var(--border-color);
      white-space: normal;
      overflow-wrap: break-word;
    }

    th {
      background-color: light-dark(var(--accent4), var(--accent));
      color: var(--text);
      font-weight: bold;
    }

      /* Alternating row colors */
    tr:nth-child(even) {
      background-color: light-dark(#f9f9f9, var(--action-item-background-dark));
    }

    tr:nth-child(odd) {
      background-color: light-dark(var(--neutral-white), var(--body-background-dark));
    }

    ul {
      list-style-type: disc;
      margin: 1em 0; /* Adds space above and below the list */
      padding-left: 40px; /* Indents the list */
    }

    li {
      margin-bottom: 0.5em; /* Adds spacing between list items */
    }

    a {
      color: var(--link-font-color);
    }
  </style>
  <script>
    document.addEventListener("DOMContentLoaded", function () {
      document.querySelectorAll("table").forEach((table) => {
        const firstRow = table.querySelector("tr");
        if (!firstRow) return;

        const colCount = firstRow.children.length;
        const equalWidth = 100 / colCount + "%";

        table.style.tableLayout = "fixed"; // Ensures equal columns
        table.querySelectorAll("th, td").forEach((cell) => {
          cell.style.width = equalWidth;
        });
      });
    });

  </script>
</head>
<body>
{{> header-content}}
<div class="privacy">
  <p class="loading" id="loading-message">Loading privacy policy...</p>
  <div id="privacy-policy"></div>
</div>

<script>
  fetch("https://hazelcast.com/wp-json/wp/v2/pages?slug=privacy")
    .then(response => response.json())
    .then(data => {
      if (data.length > 0) {
        document.getElementById("privacy-policy").innerHTML = data[0].content.rendered;
      } else {
        document.getElementById("privacy-policy").innerHTML = "<p class='error'>Privacy policy not found.</p>";
      }
      document.getElementById("loading-message").style.display = "none";
    })
    .catch(error => {
      console.error("Error fetching privacy policy:", error);
      document.getElementById("privacy-policy").innerHTML = "<p class='error'>Error loading privacy policy.</p>";
      document.getElementById("loading-message").style.display = "none";
    });
</script>

<script src="{{{uiRootPath}}}/js/site.js"></script>

</body>
</html>
